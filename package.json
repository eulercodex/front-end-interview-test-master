{
  "name": "front-end-interview-test-master",
  "version": "1.0.0",
  "description": "front end interview master server",
  "main": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "mock-db-server": "cd front-end-interview-test-api && yarn install && npm run check",
    "start-react-app-dev": "cd front-end-interview-test-client && yarn install && yarn start",
    "dev": "concurrently --kill-others-on-fail \"yarn mock-db-server\" \"yarn start-react-app-dev\""
  },
  "author": "Jesse",
  "license": "UNLICENSED",
  "devDependencies": {
    "concurrently": "^4.1.0"
  }
}
